import { Route } from '@angular/router';
import { SetupComponent } from './setup/setup.component';

export const triviaFeatureSetupRoutes: Route[] = [
  {
    path: '',
    component: SetupComponent
  }
];
